import type { FC } from 'react';

import { Section } from '../../../shared/components/Section';

export const Structure: FC = () => {
	return (
		<Section
			sectionWidth='full'
			sectionTitle={{ text: 'Структура подразделения' }}></Section>
	);
};
